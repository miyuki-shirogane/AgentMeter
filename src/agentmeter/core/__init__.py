"""Framework-agnostic core models.

Import directly from the submodules, e.g. ``from agentmeter.core.trace import
Trace``. The public API is re-exported from the top-level ``agentmeter``
package to avoid eager imports that could cause circular dependencies.
"""
